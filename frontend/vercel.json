{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install && npm run build",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ],
  "env": {
    "NEXT_PUBLIC_USE_MOCK_API": "true"
  }
}
